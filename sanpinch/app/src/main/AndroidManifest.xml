<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sanpinch">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GamePlayerActivity"></activity>
        <activity android:name=".GameKeeperActivity" />
        <activity android:name=".ScriptDetailActivity" />
        <activity android:name=".JoinActivity" />
        <activity android:name=".ScriptmngActivity" />
        <activity android:name=".HostActivity" />
        <activity android:name=".Pcmng_activity" />
        <activity
            android:name=".Home_Activity"
            android:persistent="true" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>